interface RatingProps{
    rating: number,
    size: number,
    showAll: boolean,
    includeLink: boolean
}


function RatingButtons({rating = 0, size = 24, showAll = true, includeLink= true} : RatingProps) {
    
}
export default RatingButtons