fiservice
delete fic entry
add new

settingservice
create new user settings obj
delere yser settings, and associated fics

conponent
button with flaot group to input a url, does fetch request, on return, pop fic modal.

fic modal,
add resync button,
add remove from db button

export/import panel

user pref panel,
add login/out stuff

build widget component(show stuff based on url handed to it)

add local storage support
after that....
how to integrate as plugin??
