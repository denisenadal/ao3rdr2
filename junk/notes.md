syncing updates to notes and tags to database,
syncing updates to read, rating
syncing settings to database.

button with flaot group to input a url, does fetch request, on return, pop fic modal.

fic modal,
add resync button, add remove from db button

user pref panel, add login/out stuff

build widget component(show stuff based on url handed to it)

add local storage support
after that....
how to integrate as plugin??
