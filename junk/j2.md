const handleRatingChange=(e:MouseEvent,fic:Fic)=>{
  let newRating = parseInt(e.target.value || "0");
  const oldRating = fic.rating;
  if(oldRating === newRating){
    newRating = 0;
  }
   let updatedFic: Fic = {...fic, rating: newRating }
  updateSelectedFic(updatedFic);
}